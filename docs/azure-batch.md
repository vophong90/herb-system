# Azure Batch (Planned)

We will run docking as containerized tasks:
- A Batch Pool with container support
- Job per docking request (or per batch of compounds)
- Task runs services/docking Docker image
Inputs/outputs stored in Azure Blob Storage
